# Escalated Spring — Docker demo (scaffold, not end-to-end)

Draft. Builds the Spring Boot jar with `./gradlew bootJar` and runs it against Postgres 16. Spring Security + JPA need a demo `User`, seeder, and `/demo` click-to-login controller to complete the end-to-end flow. Probably want the gem/lib to expose a starter profile.
